#extend("base")

#export("body") {
  <table class="responsive-table highlight">
    <thead>
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      #loop(genres, "genre") {
        <tr>
          <td>#(genre.name)</td>
        </tr>
      }
    </tbody>
  </table>
}
